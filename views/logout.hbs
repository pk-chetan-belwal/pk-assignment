<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Logged Out</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <style>
        body {
            font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
            background: #f7f9fc;
            color: #222;
            margin: 0;
            height: 100vh;
            display: flex;
            align-items: center;
        }
        .card {
            background: #fff;
            padding: 2rem 2.5rem;
            border-radius: 8px;
            box-shadow: 0 6px 18px rgba(15, 23, 42, 0.08);
            text-align: center;
            max-width: 420px;
            width: 90%;
            margin-left: 350px;
        }
        h1 { margin: 0 0 0.5rem; font-size: 1.25rem; }
        p { margin: 0 0 1rem; color: #555; }
        a.button {
            display: inline-block;
            padding: 0.5rem 1rem;
            background: #2563eb;
            color: #fff;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
        }
        a.button:hover { background: #1e40af; }
    </style>
</head>
<body>
    <div class="card">
        <h1>You have been successfully logged out</h1>
        <p>If you need to sign in again, click the button below.</p>
        <a class="button" href="/auth/login">Log in</a>
    </div>
</body>
</html>